# MongoDB Atlas Connection String
# Get this from: Atlas → Connect → Drivers → Node.js
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/sendo_fleet?retryWrites=true&w=majority

# Port (Render sets this automatically)
PORT=3000
